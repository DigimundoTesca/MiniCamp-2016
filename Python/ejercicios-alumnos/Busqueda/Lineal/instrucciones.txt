existen listas L[]

L=[0,2,4,False,'1212']

L.find(n)
donde n es el valor a buscar

L.insert(n,m)
donde n es el valor a buscar, y m es el valor a sustituir

- Van a buscar un elemento en una lista sin el metodo find. Tienen que crear un metodo

	result = mibusqueda(L)


- Van a crear el metodo insert, obvio :v sin insert XD 


def busqueda(lista):
	
	# retorna la posición si el elemento existe
	# retorna -1
	return ()

